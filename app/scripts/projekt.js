// Mixitup Filter Gallery
jQuery(document).ready(function ($) {
    $('.slider').slick({
        centerMode: true,
        centerPadding: '10px',
        slidesToShow: 1,
        dots: true,
        infinite: true,
        speed: 500,
        fade: true,
        arrows: true,
        useCSS: true

    });
});
